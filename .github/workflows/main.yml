name: Rashmi's sample

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Print a HelloRashmi message
        run: echo "Hello from GitHub Actions!"

      # You can add more steps here based on your requirements.
